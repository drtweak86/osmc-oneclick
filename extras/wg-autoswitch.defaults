# /etc/default/wg-autoswitch style defaults (also used by the script if present in repo)
# Tuning
MIN_DL_MBIT=12
MAX_RTT_MS=80
MAX_LOSS_PCT=2
TEST_BYTES=$((4*1024*1024))   # ~4 MB per probe

# Logging & notifications
LOG_FILE=/var/log/wg-autoswitch.log
SHOW_NOTIFY=1
NOTIFY_ICON="🦈"
# Map of flag icons (best-effort)
FLAG_de-ber="🇩🇪"
FLAG_nl-ams="🇳🇱"
FLAG_uk-lon="🇬🇧"
FLAG_uk-man="🇬🇧"
FLAG_us-nyc="🇺🇸"
